datamodule:
  _target_: src.data.vcc_embedding_module.VCCDataModule
  train_exp_data_path: ${paths.data_dir}/log-processed-data/training_data-counts.parquet
  gene_embedding_path: ${paths.data_dir}/gene_embeddings/quantiles-train_expression.parquet
  train_ko_data_path: ${paths.data_dir}/processed-data/training_data-row_metadata.csv
  control_data_path: ${paths.data_dir}/log-processed-data/control_exp_data.parquet
  test_ko_data_path: ${paths.data_dir}/processed-data/pert_counts_Validation.csv
  test_exp_data_path: ${paths.data_dir}/log-processed-data/control_exp_data_uint.parquet # Default path
  seed: ${seed}
  num_workers: 8
  batch_size: 128
  test_size: 0.2

metadata:
  groupname: VCC
  savename: ${data.metadata.groupname}_${now:%d_%m_%Y}/${now:%H-%M} # Always with respect to the top config
#
